name: Build Vajra OS ISO

on:
  push:
    tags:
    - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag'
        required: false
        default: '1.0'

jobs:
  build-iso:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up build environment
        run: |
          sudo apt-get update
          sudo apt-get install -y debootstrap squashfs-tools xorriso grub-pc-bin grub-efi-amd64-bin mtools dosfstools isolinux syslinux-common live-build debian-archive-keyring
      - name: Build Vajra OS ISO
        working-directory: live-build
        run: |
          chmod +x auto/config
          lb config
          sudo lb build 2>&1 | tee build.log
          VERSION="${{ github.event.inputs.version || '1.0' }}"
          for f in *.hybrid.iso *.iso; do
            if [ -f "$f" ]; then
              mv "$f" "vajra-os-${VERSION}-amd64.iso"
              break
            fi
          done
          if [ -f "vajra-os-${VERSION}-amd64.iso" ]; then
            sha256sum "vajra-os-${VERSION}-amd64.iso" > "vajra-os-${VERSION}-amd64.iso.sha256"
            echo "BUILD_SUCCESS=true" >> $GITHUB_ENV
          fi
      - name: Upload ISO artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: vajra-os-iso
          path: live-build/*.iso
          retention-days: 90
      - name: Create Release
        if: env.BUILD_SUCCESS == 'true' && startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: |
            live-build/vajra-os-*-amd64.iso
            live-build/vajra-os-*-amd64.iso.sha256
          body: |
            ## Vajra OS Download
            1. Download the ISO file
            2. Flash to USB (use Rufus on Windows, dd on Linux/Mac)
            3. Boot from USB
            4. Follow installer
#!/usr/bin/make -f
%:
	dh $@

override_dh_auto_install:
	install -d $(CURDIR)/debian/vajra-keyring/usr/share/keyrings
	install -m 644 $(CURDIR)/src/vajra-archive-keyring.gpg \
		$(CURDIR)/debian/vajra-keyring/usr/share/keyrings/vajra-archive-keyring.gpg

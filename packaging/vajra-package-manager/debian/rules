#!/usr/bin/make -f
%:
	dh $@

override_dh_auto_install:
	install -d $(CURDIR)/debian/vajra-package-manager/usr/bin
	install -m 755 $(CURDIR)/src/vajra-package-manager.py \
		$(CURDIR)/debian/vajra-package-manager/usr/bin/vajra-app-store
